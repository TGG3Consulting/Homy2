# Homy — Единый план работ (оба аудита сведены)

Источники: `AUDIT-backend-frontend-2026-07.md` (контракт FE↔BE, моки, конфликты логики, мёртвый код) + `AUDIT-admin-coverage-2026-07.md` (что нельзя сделать через админку). Пересечения объединены, ID сохранены для трассировки. Порядок = порядок исполнения. Ничего не выкинуто.

**Легенда статуса:** ☐ не начато · ⧗ в работе · ✅ готово
**Принцип (требование заказчика):** любое управляющее действие делается через админку; ничего — через БД или прямой API.

---

## ФАЗА 0 — Безопасность (в первую очередь)

- ✅ **0.1 — C1. Запрет самоназначения `user_type=admin/consultant`.** `lib/auth/userTypes.ts` + `users/me` PATCH + `users/me/preferences` PUT. Проверено: 400 на consultant/admin, support‑inbox 403 агенту. *(Готово, ждёт коммита.)*
- ✅ **0.2 — C2. Аутентификация WebSocket `/ws/chat`.** upgrade проверяет `homy_access_token`, привязывает `userId` (`AiWs`); анон — лимит 2 сообщения/24ч на IP, дальше `auth_required`. Проверено вживую (WS отдаёт `ready`, userId прикрепляется).

---

## ФАЗА 1 — Данные и корректность (доверие/логика)

- ✅ **1.1 — INT‑1. Intelligence/Opinion не фабрикуют факты.** Убран `Math.random()` и хардкод‑таблица района из `propertyIntelligence.ts`; юр.чистота/двойная продажа/ownership/ROI = null; остальное честно из реальных `nearby` POI. Непроверяемые поля помечены «источник в подключении» (по решению заказчика). Проверено в UI.
- ✅ **1.2 — B3+C3. Единый источник `match_score`.** Решение заказчика: на `/results` авторитет — **оценки AI** (`ai_match_scores`), заморожены на состояние запроса (кеш браузера `sessionStorage`), меняются только при переподборе AI. Durable — только при явном «Сохранить поиск» (снимок `properties` уже несёт `match_score`, без миграции). Каталог `/api/properties`: детерминированный скор **до** пагинации (score‑sort скорит весь набор → сортирует → пагинирует; price/date — DB‑пагинация + скор без переупорядочивания); статик‑колонка БД снята с ранжирования; `is_top_choice` = артефакт ранкера (сброс в `enrichWithAI`). Убран FE‑фолбэк `||85` (`ChatContainer`). Открытие сохранённого поиска — из снимка (`/results?savedId=`), без AI‑переоценки. Проверено e2e: топ единственный, скор до пагинации, заморозка переживает релоад (91/77), обычный AI‑поиск не сломан.
- ⧗ **1.3 — B7+C4. Нормализация i18n.** ✅ Скоринг/фильтр переведён на скаляр‑ключ `district` (не JSON‑`neighborhood`): `matchScoreService` 4×, `voice/route` и `savedSearchMatcher` → `where.district` (заодно устранён баг перетирания `where.OR` в voice). ✅ Валютный дрейф: `PropertyListing.currency` default USD→AMD (нужна миграция). ✅ Канонический `loc` централизован в `lib/i18n`. `title/neighborhood` остаются локализованным JSON (только для отображения, через `loc`/`getLocalized`; `orderBy` по ним нет — ОК). ✅ СДЕЛАНО: 10 файлов с локальным `loc` (тела байт-в-байт идентичны каноническому) переведены на `import { useT, loc } from '@/lib/i18n'` — локальные копии удалены. Сделано Python-скриптом с сохранением EOL (не sed). tsc 0 ошибок, /allresults рендерит локализованные заголовки. (`properties/list` — там `loc` это локальная переменная адреса, не функция, не трогали.)
- ✅ **1.4 — H1. `include: owner` в карточке объекта.** GET `properties/[id]` теперь `include: owner` (id, first_name, last_name, user_type); `setProperty` копирует `verified`; бейдж «Проверен» переведён с хрупкого `contact?.verified` на авторитетный `Property.verified` (ставится на approve). Проверено вживую: рендерится «Anna Hakobyan · Проверен · агент». Карточки списков (favorites/recommendations) owner не показывают, а детальная всегда идёт через [id] — расширять адаптер не требуется.
- ✅ **1.5 — ПЕРЕОСМЫСЛЕНО (правило заказчика): отзывы у БРОКЕРА, не у объекта.** У недвижимости отзывов быть не должно. Сделано: `Review` перенацелен с `property_id` на `agent_id` (миграция `reviews_target_broker`); User — две связи (получил/написал), Property — отзывы убраны. Новый `/api/agents/[id]/reviews` (GET stats+имена авторов first/last_name — заодно закрыт M1; POST 1/автор, нельзя себя). Старый `/api/properties/[id]/reviews` → 410. Карточка объекта: секция «Отзывы» удалена; в блоке агента — звёзды+кол‑во отзывов брокера и «Оценить брокера». `reviews/[id]` PATCH/DELETE (author-scoped) работают. Проверено e2e: POST 201 → 5.0/1, рендер в блоке агента, DELETE → 0, старый роут 410. Правило записано в память.
- ✅ **1.6 — ПЕРЕОСМЫСЛЕНО под client-request модель.** ViewingSlot был полностью мёртв (сервис звался только из своего API, страница его результат отбрасывала из-за несовпадения формата, сид генерил слоты впустую, bookSlot/generateSlots не вызывались). Удалено целиком: модель `ViewingSlot` + 2 связи (User/Property), сервис `lib/services/viewingSlot.ts`, API `viewing-slots/route.ts`, весь ViewingSlot-код из сида. Страница `schedule` больше не делает фейковый фетч слотов — честно предлагает набор желаемых времён (клиент выбирает предпочтительное → `POST /api/viewing/schedule` создаёт `Viewing` со статусом `pending_agent` → агент подтверждает/предлагает другое). Текст успеха честнее: «Заявка отправлена… агент подтвердит или предложит другое». Проверено вживую (рендер + чистая консоль). **Нужна миграция `drop_viewing_slots`.**
- ✅ **1.7 — B4. Переход просмотра в `completed`.** Добавлен cron‑роут `POST /api/cron/complete-viewings` (та же конвенция `x-cron-secret`, что у saved‑search): `confirmed` просмотры с прошедшим `scheduledAt` → `completed`. Теперь счётчики completed (admin/dashboard, platform/metrics, buyer dashboard) станут реальными. Проверено: 200 `{completed:0}`. Триггерить внешним планировщиком (как saved‑search cron). Ручное «завершить» админом/агентом придёт с 2.5.
- ✅ **1.8 — B6. Lead stage «прилипает».** PATCH `agent/leads/[id]`: при ручной установке active‑стадии (new/warm) теперь бампается `last_contact_at=now`, иначе `effectiveStage()` сразу возвращал бы `cold`. Проверено на агенте (Anna): cold→PATCH warm→остаётся warm; возвращено в cold.
- ✅ **1.9 — B1+B2. Ось авторизации.** `app/dashboard/page.tsx` — порядок переставлен: сначала `role` (admin/moderator) → AdminPanel, затем `user_type` agent/owner → BrokerCabinet, затем buyer/renter/default → BuyerDashboard. Теперь админ с дефолтным/пустым `user_type` не проваливается в кабинет покупателя. Проверено: `/api/users/me` отдаёт `role`, админ → AdminPanel (без регрессии).

---

## ФАЗА 2 — Покрытие админкой (убираем костыли; API‑действия — в UI)

- ⧗ **2.1 — Управление пользователями** *(закрывает хвост C1)*. ✅ `set_user_type` в `admin/users` PATCH (buyer/renter/owner/agent/consultant, лог + user_type в ответе) + UI‑селектор персоны в «Пользователи» → назначение/снятие консультанта через панель. Проверено e2e (agent→consultant→revert). ✅ `update_profile` (имя/фамилия/телефон) + `verify_email` (ручное подтверждение) — действия в PATCH + модалка «Редактировать» и кнопка «Подтвердить email» в UI; проверено e2e (правка телефона + revert, verify 200). ✅ СДЕЛАНО: создание/удаление пользователя + force-reset. Добавлен `token_version` в User (миграция `add_user_token_version`) — каждый access/refresh токен несёт версию; auth-middleware (withAuth/withOptionalAuth/withBroker/withAdmin/withModerator) отклоняет токен, если версия != текущей → **мгновенный выход со всех устройств + мгновенная блокировка**. login/refresh кладут версию; reset-password бампит её (смена пароля = выход). admin/users: `POST` (создание — юзер сам ставит пароль по reset-ссылке, email + фолбэк-ссылка админу), `DELETE` (hard-delete, только role=user, каскад заявок/объявлений/объектов/лидов; админа/модератора нельзя — только блок/снятие роли), `PATCH force_reset` (обнуляет старый пароль + бампит версию + reset-ссылка). UI: кнопка «Создать пользователя» + в строке правка/блок/ключ(reset)/корзина. e2e (все зелёные): регресс авторизации админа и юзера ок, старый пароль после сброса → 401, hard-delete чистит и оставляет 0.
- ✅ **2.2 — Управление объектами.** (a) Админ‑оверрайд в `properties/[id]` PATCH+DELETE (по `role`); `/api/admin/properties` GET (все объекты, вкл. снятые) + страница `/admin/properties` (правка address/price, публикация/снятие, удаление) + навигация «Properties». Проверено: админ снял/опубликовал/удалил чужой объект. (b) A5 — теряемые при approve поля аренды (`deposit_months`, `utilities_estimate`, `minimum_lease_months`) добавлены в `PropertyListing` (миграция `listing_rental_terms`), собираются в форме заявки (3 инпута для аренды) во всех путях (create/edit заявки/edit объекта) и переносятся в `Property` на approve. Проверено e2e (аренда→approve→объект несёт поля). Virtual tour — отдельно (2.9).
- ✅ **2.3 — Правка заявки до одобрения.** Админ‑оверрайд в `properties/listings/[id]` PATCH+DELETE (`isAdminUser` по `role`). Кнопка «Изменить» + модалка (локация/цена/площадь/комнаты/описание) в `/admin/moderation` рядом с Approve/Reject. «Запросить доработку» = существующий reject с причиной (owner видит `rejection_reason`). Проверено e2e (модератор изменил price/rooms/описание → сохранилось; кнопка+модалка в UI).
- ✅ **2.4 — Модерация отзывов.** Новый `/api/admin/reviews` (withModerator): GET список брокер‑отзывов (агент+автор+рейтинг+коммент, фильтр `max_rating`, пагинация) + DELETE любого (лог `review_delete`). Страница `/admin/reviews` (таблица + фильтр «≤2★» + удаление с подтверждением) + пункт навигации «Reviews» в admin layout. Проверено e2e (создать отзыв → виден у админа → фильтр → удалить через UI → 0).
- ✅ **2.5 — Обзор просмотров (админ)** + пометка `completed` (см. 1.7). Новый `/api/admin/viewings` (withModerator): GET все (объект+клиент+агент+время+статус, фильтр статуса, пагинация) + PATCH complete/cancel (лог `viewing_complete`/`viewing_cancel`). Страница `/admin/viewings` (таблица, локализованный тайтл через `loc`, бейджи статусов, фильтр, кнопки Завершить/Отменить) + пункт навигации «Viewings». Проверено e2e (создать→complete→cancel, рендер строки). Перенос времени — отдельно (не критично).
- ✅ **2.6 — Обзор лидов/сделок (админ).** `/api/admin/leads` (GET все лиды с effectiveStage + агент/клиент/объект; PATCH reassign — переназначение на другого агента/владельца/консультанта, лог) + `/api/admin/deals` (GET все сделки). Страница `/admin/crm` с вкладками Лиды/Сделки (переназначение через селектор агента, локализованные тайтлы, бейджи стадий/статусов) + пункт навигации «CRM». Проверено e2e (reassign Anna→Karine→revert, invalid→404, рендер обеих вкладок).
- ✅ **2.7 — Десктоп поддержки/консультантов (админ).** `/api/admin/support` (withModerator): GET support‑диалоги (клиент/консультант/статус/кол‑во сообщений) + ростер консультантов (`is_online` + нагрузка через groupBy назначенных) ; PATCH назначение/снятие диалога конкретному консультанту (лог). Страница `/admin/support` (ростер онлайн/нагрузка + таблица диалогов с селектором назначения) + навигация «Support». Проверено e2e (сделал консультанта → назначил диалог → status=assigned, load=1 → снял). Капасити‑лимит — отдельно (нет поля в схеме).
- ✅ **2.8 — Waitlist.** `/api/admin/waitlist` GET (withModerator, все записи email+дата). Страница `/admin/waitlist` (таблица + «Экспорт CSV» клиентом) + навигация. Проверено e2e (signup → виден у админа, экспорт‑кнопка).
- ✅ **2.9 — Авторинг виртуальных туров** (A4). Бэкенд: `POST /api/properties/[id]/virtual-tour` (создать комнату), `PATCH+DELETE …/[roomId]` (править name×3/panorama/order/hotspots; удалить), `GET ?manage=true` (все комнаты для owner/admin), тумблер `virtual_tour_enabled` в `properties/[id]` PATCH; всё owner/admin‑scoped. Визуальный редактор `/tour-editor/[propertyId]`: список комнат (добавить/удалить), 360°‑панорама (Three.js), клик по панораме → хотспот (экранные x/y, совместимо со вьювером) → выбор целевой комнаты, метки‑переходы (клик — удалить), тумблер «включить тур». Кнопка «Тур» в `/admin/properties`. Проверено e2e (create/patch‑hotspot/enable/delete; редактор рендерит 5 сид‑комнат + панораму; UI create/delete). Хотспоты — экранные проценты (как в существующем вьювере), а не 3D‑якорь.
- ✅ **2.10 — Системные/broadcast‑уведомления** из админки. `/api/admin/notifications` POST (withModerator): рассылка `system`‑уведомления всем или по персоне (buyer/renter/owner/agent/consultant) через `createMany` + лог, возврат count. Страница `/admin/notifications` (форма: аудитория/заголовок/текст) + навигация «Notifications». Проверено e2e (broadcast «owner» → sent=4).
- ✅ **2.11 — `PlatformMetric`** (A2): удалена мёртвая модель (миграция `drop_platform_metrics`) + убрана ссылка в seed. Убраны хардкод‑фолбэки 1500/15/350 в `/api/platform/metrics` (норм‑путь → реальные числа; error → честный 500 вместо фейков). Проверено: метрики реальные (properties=20, cities=10, deals=0).

---

## ФАЗА 3 — Чистка и тех.долг

- ✅ **3.1 — Слить две админки.** Единая админка — `/admin/*` (полный набор разделов). `/dashboard` теперь редиректит admin/moderator на `/admin` (useEffect + `router.replace`; ветка рендера → `null`); импорт `AdminPanel` убран. Проверено: `/dashboard`(admin) → `/admin`. `components/homy/AdminPanel.tsx` больше не используется (мёртвый код — удалить в дочистке).
- ⧗ **3.2 — Убрать дубли эндпоинтов.** ✅ favorites: `RecommendedProperties` переведён на канонический `/api/users/me/favorites` (body `{propertyId}`); `/api/favorites`(+`[id]`) → 410. Проверено (410 старый / add‑list‑delete канонический). ✅ Мёртвые дубли (0 вызывающих) → 410: `auth/me` (твин `users/me`), `users/me/viewings` (→`viewings`), `users/me/preferences` (→ PATCH `users/me`), `viewings/[id]/reject` (= cancel). ✅ СДЕЛАНО: `viewing/schedule` vs `viewings` (B5/C5) консолидированы. Клиентская ветка `/api/viewings` оказалась эквивалентным суперсетом (та же 409-проверка, статус `pending_agent`, тип `viewing_request`) + богаче (email/notification/include). `schedule/page` перенаправлена на `/api/viewings` (camelCase-ключи); `viewing/schedule` → 410. Важно: `/api/viewings` НЕ создавал CRM-лид, а `viewing/schedule` создавал → был рассинхрон (заявка через форму не попадала в CRM). Добавлен `captureLead` в клиентскую ветку `/api/viewings` + расширены select-ы (property: price/rooms/bedrooms/district/neighborhood; user: first/last_name/phone) → полноценный лид. e2e (admin как клиент): POST 200 `pending_agent`, лид «6-комн · Kentron · 450 000 000 AMD» создан, заявка отменена. tsc чист.
- ✅ **3.3 — Split‑brain `Property` vs `PropertyListing`.** Граница чистая и намеренная: `PropertyListing` = заявка (pending) → admin approve → `Property` = live. Лишний путь `POST /api/users/me/properties` создавал `Property` напрямую (`available:true`, **мимо модерации**) и не имел вызывающих → заглушён (410); GET оставлен (нужен `ViewingCreateForm`). Остался один канонический путь создания: `POST /api/properties/list` → модерация → approve. `properties/listings/[id]` — это правка заявки, не создание. Проверено: POST→410, GET→200, канонический путь цел. Слияние двух моделей не требуется (граница submission/live корректна).
- ✅ **3.4 — Мёртвый код/идеи.** `consultant/assigned` → честный `{consultant:null}` (убран хардкод Anna, ни один экран не читал); `agent/contact` → 410 (фейк `console.log`, без вызывающих). `lib/api/client.ts` мёртвые методы (`featured/recent/neighborhoods/stats`) оставлены — безобидны, файл используется для `api.auth.*`. **`OauthAccount` — ждёт решения пользователя:** модель + связь `User.oauthAccounts`, 0 упоминаний в коде → удалить миграцией (как PlatformMetric) либо оставить под будущий OAuth.
- ✅ **3.5 — Плейсхолдеры дашбордов.** `search_history_count` → count `SavedSearch`; `deals_closed` → count `Deal status='won'`; `total_views` → новая модель `PropertyView` (лог просмотров) + fire-and-forget запись в `GET /api/properties/[id]` + счёт за 7 дней, исключая владельца (мокап «Просмотры за 7 дней»). Метка `totalViews` обновлена в ru/en/hy. **Нужна миграция `add_property_views`.**
- ✅ **3.6 — Enum `Notification.type`** (A6): единый источник `lib/constants/notifications.ts` (11 реальных типов: viewing_request/proposed/confirmed/cancelled, listing_approved/rejected, chat_new/message/assigned, saved_search_match, system + `notificationCategory()`). Коммент схемы обновлён под реальность (было устаревшее viewing_approved/message). FE `iconFor` переписан через категории — починен чат (мёртвая ветка 'message'), добавлены раздельные иконки confirmed/cancelled. Проверено вживую. `broadcast`/`notification_broadcast` — это поля adminActionLog, не типы уведомлений.
- ✅ **3.7 — Мёртвые/раздвоенные колонки** (A7): `isTopChoice` → колонка УДАЛЕНА (топ-выбор per-search рантайм; адаптер отдаёт false, ранкер ставит). `warning` → колонка авторитетна, убрана рантайм-генерация `generateWarning` из адаптера (расходилась список↔деталка + сломанный hy); метод удалён. Сид очищен от isTopChoice (26 ссылок). **Бонус:** убран осиротевший `platformMetric`-блок из сида (недочистка 2.11 — сид падал на удалённой модели). **Нужна миграция `drop_property_is_top_choice`.** Найдено попутно (не чинил): латентные tsc-ошибки `dashboard/page.tsx role` (из 3.1, рантайм ок), `register ref`, осиротевший `public/seed_export.ts`.

---

## Матрица приоритетов (что за чем)

| Приоритет | Пункты | Почему |
|---|---|---|
| P0 сейчас | 0.1✅, 0.2 | Безопасность/расходы |
| P1 | 1.1, 1.2, 1.3, 1.4, 1.9 | Доверие и базовая корректность (скор, i18n, роли, факты) |
| P1→P2 мост | 2.1 | Прямой костыль от C1 (провижининг консультанта) |
| P2 | 2.2–2.7, 1.5–1.8 | Полное управление платформой через админку |
| P3 | 2.8–2.11, 3.1–3.7 | Дочистка, дубли, мёртвый код |

**Зависимости:** 1.7↔2.5 (completed); 2.1↔2.7 (консультанты); 1.6↔A3 (слоты); 2.2↔A5 (поля объекта); 3.2/3.3 делать после того, как решено, какая модель/эндпоинт канонический.

*Следующий шаг по умолчанию:* закоммитить 0.1 (C1), затем 0.2 (C2), затем 2.1 (управление пользователями — закрывает костыль консультанта).
